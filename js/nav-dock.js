/* Shared navigation — no-op (legacy dock slider removed in nav overhaul) */
(function () {
    /* The liquid-dock + slider has been replaced with a flat nav-links bar.
       This file is kept to avoid 404s from any <script> tags still referencing it. */
})();
